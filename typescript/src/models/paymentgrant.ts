/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";
import {
  PaymentGrantAttestation,
  PaymentGrantAttestation$inboundSchema,
} from "./paymentgrantattestation.js";
import {
  PaymentGrantLimits,
  PaymentGrantLimits$inboundSchema,
} from "./paymentgrantlimits.js";
import {
  PaymentGrantScope,
  PaymentGrantScope$inboundSchema,
} from "./paymentgrantscope.js";

export type PaymentGrant = {
  grantId: string;
  tenantId: string;
  agentPassportId: string;
  runId: string;
  scope: PaymentGrantScope;
  limits: PaymentGrantLimits;
  attestation: PaymentGrantAttestation;
  /**
   * Ed25519 signature (hex)
   */
  signature: string;
  /**
   * Ed25519 public key (hex)
   */
  signerPubkey: string;
};

/** @internal */
export const PaymentGrant$inboundSchema: z.ZodMiniType<PaymentGrant, unknown> =
  z.pipe(
    z.object({
      grant_id: types.string(),
      tenant_id: types.string(),
      agent_passport_id: types.string(),
      run_id: types.string(),
      scope: PaymentGrantScope$inboundSchema,
      limits: PaymentGrantLimits$inboundSchema,
      attestation: PaymentGrantAttestation$inboundSchema,
      signature: types.string(),
      signer_pubkey: types.string(),
    }),
    z.transform((v) => {
      return remap$(v, {
        "grant_id": "grantId",
        "tenant_id": "tenantId",
        "agent_passport_id": "agentPassportId",
        "run_id": "runId",
        "signer_pubkey": "signerPubkey",
      });
    }),
  );

export function paymentGrantFromJSON(
  jsonString: string,
): SafeParseResult<PaymentGrant, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => PaymentGrant$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'PaymentGrant' from JSON`,
  );
}
