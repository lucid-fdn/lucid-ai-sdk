/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import * as types from "../../types/primitives.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import * as models from "../index.js";

/**
 * OK
 */
export type LucidListPassportsPendingSyncResponse = {
  success: boolean;
  count: number;
  passports: Array<models.Passport>;
};

/** @internal */
export const LucidListPassportsPendingSyncResponse$inboundSchema: z.ZodMiniType<
  LucidListPassportsPendingSyncResponse,
  unknown
> = z.object({
  success: types.boolean(),
  count: types.number(),
  passports: z.array(models.Passport$inboundSchema),
});

export function lucidListPassportsPendingSyncResponseFromJSON(
  jsonString: string,
): SafeParseResult<LucidListPassportsPendingSyncResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) =>
      LucidListPassportsPendingSyncResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'LucidListPassportsPendingSyncResponse' from JSON`,
  );
}
