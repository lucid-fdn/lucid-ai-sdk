/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { ClosedEnum } from "../../types/enums.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import * as types from "../../types/primitives.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * Deployment provider name
 */
export const Provider = {
  Railway: "railway",
  Akash: "akash",
  Phala: "phala",
  Ionet: "ionet",
  Nosana: "nosana",
} as const;
/**
 * Deployment provider name
 */
export type Provider = ClosedEnum<typeof Provider>;

export type LucidReceiveDeploymentWebhookRequest = {
  /**
   * Deployment provider name
   */
  provider: Provider;
  body: { [k: string]: any };
};

/**
 * Webhook processed
 */
export type LucidReceiveDeploymentWebhookResponse = {
  success: boolean;
  eventId?: string | undefined;
};

/** @internal */
export const Provider$outboundSchema: z.ZodMiniEnum<typeof Provider> = z.enum(
  Provider,
);

/** @internal */
export type LucidReceiveDeploymentWebhookRequest$Outbound = {
  provider: string;
  body: { [k: string]: any };
};

/** @internal */
export const LucidReceiveDeploymentWebhookRequest$outboundSchema: z.ZodMiniType<
  LucidReceiveDeploymentWebhookRequest$Outbound,
  LucidReceiveDeploymentWebhookRequest
> = z.object({
  provider: Provider$outboundSchema,
  body: z.record(z.string(), z.any()),
});

export function lucidReceiveDeploymentWebhookRequestToJSON(
  lucidReceiveDeploymentWebhookRequest: LucidReceiveDeploymentWebhookRequest,
): string {
  return JSON.stringify(
    LucidReceiveDeploymentWebhookRequest$outboundSchema.parse(
      lucidReceiveDeploymentWebhookRequest,
    ),
  );
}

/** @internal */
export const LucidReceiveDeploymentWebhookResponse$inboundSchema: z.ZodMiniType<
  LucidReceiveDeploymentWebhookResponse,
  unknown
> = z.pipe(
  z.object({
    success: types.boolean(),
    event_id: types.optional(types.string()),
  }),
  z.transform((v) => {
    return remap$(v, {
      "event_id": "eventId",
    });
  }),
);

export function lucidReceiveDeploymentWebhookResponseFromJSON(
  jsonString: string,
): SafeParseResult<LucidReceiveDeploymentWebhookResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) =>
      LucidReceiveDeploymentWebhookResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'LucidReceiveDeploymentWebhookResponse' from JSON`,
  );
}
