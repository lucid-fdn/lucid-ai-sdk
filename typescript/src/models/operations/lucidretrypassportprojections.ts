/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import * as types from "../../types/primitives.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import * as models from "../index.js";

export type LucidRetryPassportProjectionsRequest = {
  passportId: string;
};

/**
 * Retry triggered
 */
export type LucidRetryPassportProjectionsResponse = {
  success?: boolean | undefined;
  message?: string | undefined;
  currentRegistrations?:
    | { [k: string]: models.ExternalRegistrationStatus }
    | undefined;
};

/** @internal */
export type LucidRetryPassportProjectionsRequest$Outbound = {
  passport_id: string;
};

/** @internal */
export const LucidRetryPassportProjectionsRequest$outboundSchema: z.ZodMiniType<
  LucidRetryPassportProjectionsRequest$Outbound,
  LucidRetryPassportProjectionsRequest
> = z.pipe(
  z.object({
    passportId: z.string(),
  }),
  z.transform((v) => {
    return remap$(v, {
      passportId: "passport_id",
    });
  }),
);

export function lucidRetryPassportProjectionsRequestToJSON(
  lucidRetryPassportProjectionsRequest: LucidRetryPassportProjectionsRequest,
): string {
  return JSON.stringify(
    LucidRetryPassportProjectionsRequest$outboundSchema.parse(
      lucidRetryPassportProjectionsRequest,
    ),
  );
}

/** @internal */
export const LucidRetryPassportProjectionsResponse$inboundSchema: z.ZodMiniType<
  LucidRetryPassportProjectionsResponse,
  unknown
> = z.pipe(
  z.object({
    success: types.optional(types.boolean()),
    message: types.optional(types.string()),
    current_registrations: types.optional(
      z.record(z.string(), models.ExternalRegistrationStatus$inboundSchema),
    ),
  }),
  z.transform((v) => {
    return remap$(v, {
      "current_registrations": "currentRegistrations",
    });
  }),
);

export function lucidRetryPassportProjectionsResponseFromJSON(
  jsonString: string,
): SafeParseResult<LucidRetryPassportProjectionsResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) =>
      LucidRetryPassportProjectionsResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'LucidRetryPassportProjectionsResponse' from JSON`,
  );
}
