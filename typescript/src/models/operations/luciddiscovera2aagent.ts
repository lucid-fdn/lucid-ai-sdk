/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import * as types from "../../types/primitives.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import * as models from "../index.js";

export type LucidDiscoverA2aAgentRequest = {
  /**
   * HTTPS URL of the agent
   */
  agentUrl: string;
  /**
   * Optional auth token for the remote agent
   */
  authToken?: string | undefined;
};

/**
 * Agent discovered
 */
export type LucidDiscoverA2aAgentResponse = {
  success: boolean;
  /**
   * A2A Agent Card for agent discovery (per A2A protocol spec).
   */
  agentCard: models.A2AAgentCard;
};

/** @internal */
export type LucidDiscoverA2aAgentRequest$Outbound = {
  agent_url: string;
  auth_token?: string | undefined;
};

/** @internal */
export const LucidDiscoverA2aAgentRequest$outboundSchema: z.ZodMiniType<
  LucidDiscoverA2aAgentRequest$Outbound,
  LucidDiscoverA2aAgentRequest
> = z.pipe(
  z.object({
    agentUrl: z.string(),
    authToken: z.optional(z.string()),
  }),
  z.transform((v) => {
    return remap$(v, {
      agentUrl: "agent_url",
      authToken: "auth_token",
    });
  }),
);

export function lucidDiscoverA2aAgentRequestToJSON(
  lucidDiscoverA2aAgentRequest: LucidDiscoverA2aAgentRequest,
): string {
  return JSON.stringify(
    LucidDiscoverA2aAgentRequest$outboundSchema.parse(
      lucidDiscoverA2aAgentRequest,
    ),
  );
}

/** @internal */
export const LucidDiscoverA2aAgentResponse$inboundSchema: z.ZodMiniType<
  LucidDiscoverA2aAgentResponse,
  unknown
> = z.pipe(
  z.object({
    success: types.boolean(),
    agent_card: models.A2AAgentCard$inboundSchema,
  }),
  z.transform((v) => {
    return remap$(v, {
      "agent_card": "agentCard",
    });
  }),
);

export function lucidDiscoverA2aAgentResponseFromJSON(
  jsonString: string,
): SafeParseResult<LucidDiscoverA2aAgentResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => LucidDiscoverA2aAgentResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'LucidDiscoverA2aAgentResponse' from JSON`,
  );
}
