/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import {
  AlgorithmScore,
  AlgorithmScore$inboundSchema,
} from "./algorithmscore.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

export type CompositeReputationScoreAlgorithmScore = {
  algorithmId: string;
  weight: number;
  score: AlgorithmScore;
};

export type CompositeReputationScore = {
  overall: number;
  algorithmScores: Array<CompositeReputationScoreAlgorithmScore>;
  /**
   * Unix timestamp
   */
  computedAt: number;
};

/** @internal */
export const CompositeReputationScoreAlgorithmScore$inboundSchema:
  z.ZodMiniType<CompositeReputationScoreAlgorithmScore, unknown> = z.object({
    algorithmId: types.string(),
    weight: types.number(),
    score: AlgorithmScore$inboundSchema,
  });

export function compositeReputationScoreAlgorithmScoreFromJSON(
  jsonString: string,
): SafeParseResult<CompositeReputationScoreAlgorithmScore, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) =>
      CompositeReputationScoreAlgorithmScore$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'CompositeReputationScoreAlgorithmScore' from JSON`,
  );
}

/** @internal */
export const CompositeReputationScore$inboundSchema: z.ZodMiniType<
  CompositeReputationScore,
  unknown
> = z.object({
  overall: types.number(),
  algorithmScores: z.array(
    z.lazy(() => CompositeReputationScoreAlgorithmScore$inboundSchema),
  ),
  computedAt: types.number(),
});

export function compositeReputationScoreFromJSON(
  jsonString: string,
): SafeParseResult<CompositeReputationScore, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => CompositeReputationScore$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'CompositeReputationScore' from JSON`,
  );
}
