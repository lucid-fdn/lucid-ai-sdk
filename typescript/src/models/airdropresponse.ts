/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

export type Distribution = {
  holder?: string | undefined;
  balance?: number | undefined;
  share?: number | undefined;
  amountLamports?: number | undefined;
};

export type AirdropResponse = {
  success: boolean;
  passportId: string;
  tokenMint: string;
  /**
   * Total lamports distributed
   */
  totalDistributed: number;
  /**
   * Number of holders who received airdrop
   */
  holders: number;
  distributions: Array<Distribution>;
  txSignatures: Array<string>;
};

/** @internal */
export const Distribution$inboundSchema: z.ZodMiniType<Distribution, unknown> =
  z.object({
    holder: types.optional(types.string()),
    balance: types.optional(types.number()),
    share: types.optional(types.number()),
    amountLamports: types.optional(types.number()),
  });

export function distributionFromJSON(
  jsonString: string,
): SafeParseResult<Distribution, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Distribution$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Distribution' from JSON`,
  );
}

/** @internal */
export const AirdropResponse$inboundSchema: z.ZodMiniType<
  AirdropResponse,
  unknown
> = z.object({
  success: types.boolean(),
  passportId: types.string(),
  tokenMint: types.string(),
  totalDistributed: types.number(),
  holders: types.number(),
  distributions: z.array(z.lazy(() => Distribution$inboundSchema)),
  txSignatures: z.array(types.string()),
});

export function airdropResponseFromJSON(
  jsonString: string,
): SafeParseResult<AirdropResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => AirdropResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'AirdropResponse' from JSON`,
  );
}
