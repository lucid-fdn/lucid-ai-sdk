/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

export type Recipient = {
  address?: string | undefined;
  role?: string | undefined;
  bps?: number | undefined;
  amountLamports?: string | undefined;
};

export type AgentPayoutEntry = {
  runId: string;
  totalAmountLamports: string;
  recipients: Array<Recipient>;
  createdAt: number;
};

/** @internal */
export const Recipient$inboundSchema: z.ZodMiniType<Recipient, unknown> = z
  .pipe(
    z.object({
      address: types.optional(types.string()),
      role: types.optional(types.string()),
      bps: types.optional(types.number()),
      amount_lamports: types.optional(types.string()),
    }),
    z.transform((v) => {
      return remap$(v, {
        "amount_lamports": "amountLamports",
      });
    }),
  );

export function recipientFromJSON(
  jsonString: string,
): SafeParseResult<Recipient, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Recipient$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Recipient' from JSON`,
  );
}

/** @internal */
export const AgentPayoutEntry$inboundSchema: z.ZodMiniType<
  AgentPayoutEntry,
  unknown
> = z.pipe(
  z.object({
    run_id: types.string(),
    total_amount_lamports: types.string(),
    recipients: z.array(z.lazy(() => Recipient$inboundSchema)),
    created_at: types.number(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "run_id": "runId",
      "total_amount_lamports": "totalAmountLamports",
      "created_at": "createdAt",
    });
  }),
);

export function agentPayoutEntryFromJSON(
  jsonString: string,
): SafeParseResult<AgentPayoutEntry, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => AgentPayoutEntry$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'AgentPayoutEntry' from JSON`,
  );
}
