/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import * as openEnums from "../types/enums.js";
import { OpenEnum } from "../types/enums.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

export const EpochStatus = {
  Open: "open",
  Anchoring: "anchoring",
  Anchored: "anchored",
  Failed: "failed",
} as const;
export type EpochStatus = OpenEnum<typeof EpochStatus>;

export type Epoch = {
  epochId: string;
  projectId?: string | undefined;
  mmrRoot: string;
  leafCount: number;
  createdAt: number;
  finalizedAt?: number | undefined;
  status: EpochStatus;
  chainTx?: string | undefined;
  error?: string | undefined;
  startLeafIndex?: number | undefined;
  endLeafIndex?: number | undefined;
};

/** @internal */
export const EpochStatus$inboundSchema: z.ZodMiniType<EpochStatus, unknown> =
  openEnums.inboundSchema(EpochStatus);

/** @internal */
export const Epoch$inboundSchema: z.ZodMiniType<Epoch, unknown> = z.pipe(
  z.object({
    epoch_id: types.string(),
    project_id: types.optional(types.string()),
    mmr_root: types.string(),
    leaf_count: types.number(),
    created_at: types.number(),
    finalized_at: types.optional(types.number()),
    status: EpochStatus$inboundSchema,
    chain_tx: types.optional(types.string()),
    error: types.optional(types.string()),
    start_leaf_index: types.optional(types.number()),
    end_leaf_index: types.optional(types.number()),
  }),
  z.transform((v) => {
    return remap$(v, {
      "epoch_id": "epochId",
      "project_id": "projectId",
      "mmr_root": "mmrRoot",
      "leaf_count": "leafCount",
      "created_at": "createdAt",
      "finalized_at": "finalizedAt",
      "chain_tx": "chainTx",
      "start_leaf_index": "startLeafIndex",
      "end_leaf_index": "endLeafIndex",
    });
  }),
);

export function epochFromJSON(
  jsonString: string,
): SafeParseResult<Epoch, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Epoch$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Epoch' from JSON`,
  );
}
