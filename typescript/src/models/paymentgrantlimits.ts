/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

export type PaymentGrantLimits = {
  totalUsd: number;
  /**
   * Unix timestamp
   */
  expiresAt: number;
  maxCalls: number;
};

/** @internal */
export const PaymentGrantLimits$inboundSchema: z.ZodMiniType<
  PaymentGrantLimits,
  unknown
> = z.pipe(
  z.object({
    total_usd: types.number(),
    expires_at: types.number(),
    max_calls: types.number(),
  }),
  z.transform((v) => {
    return remap$(v, {
      "total_usd": "totalUsd",
      "expires_at": "expiresAt",
      "max_calls": "maxCalls",
    });
  }),
);
/** @internal */
export type PaymentGrantLimits$Outbound = {
  total_usd: number;
  expires_at: number;
  max_calls: number;
};

/** @internal */
export const PaymentGrantLimits$outboundSchema: z.ZodMiniType<
  PaymentGrantLimits$Outbound,
  PaymentGrantLimits
> = z.pipe(
  z.object({
    totalUsd: z.number(),
    expiresAt: z.int(),
    maxCalls: z.int(),
  }),
  z.transform((v) => {
    return remap$(v, {
      totalUsd: "total_usd",
      expiresAt: "expires_at",
      maxCalls: "max_calls",
    });
  }),
);

export function paymentGrantLimitsToJSON(
  paymentGrantLimits: PaymentGrantLimits,
): string {
  return JSON.stringify(
    PaymentGrantLimits$outboundSchema.parse(paymentGrantLimits),
  );
}
export function paymentGrantLimitsFromJSON(
  jsonString: string,
): SafeParseResult<PaymentGrantLimits, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => PaymentGrantLimits$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'PaymentGrantLimits' from JSON`,
  );
}
