/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { ClosedEnum } from "../types/enums.js";

export const ComputeWorkerStatus = {
  Online: "online",
  Offline: "offline",
  Degraded: "degraded",
  Draining: "draining",
} as const;
export type ComputeWorkerStatus = ClosedEnum<typeof ComputeWorkerStatus>;

export type ComputeWorker = {
  workerId: string;
  status: ComputeWorkerStatus;
  /**
   * Unix timestamp of last heartbeat
   */
  lastHeartbeat?: number | undefined;
};

/** @internal */
export const ComputeWorkerStatus$outboundSchema: z.ZodMiniEnum<
  typeof ComputeWorkerStatus
> = z.enum(ComputeWorkerStatus);

/** @internal */
export type ComputeWorker$Outbound = {
  worker_id: string;
  status: string;
  last_heartbeat?: number | undefined;
};

/** @internal */
export const ComputeWorker$outboundSchema: z.ZodMiniType<
  ComputeWorker$Outbound,
  ComputeWorker
> = z.pipe(
  z.object({
    workerId: z.string(),
    status: ComputeWorkerStatus$outboundSchema,
    lastHeartbeat: z.optional(z.int()),
  }),
  z.transform((v) => {
    return remap$(v, {
      workerId: "worker_id",
      lastHeartbeat: "last_heartbeat",
    });
  }),
);

export function computeWorkerToJSON(computeWorker: ComputeWorker): string {
  return JSON.stringify(ComputeWorker$outboundSchema.parse(computeWorker));
}
