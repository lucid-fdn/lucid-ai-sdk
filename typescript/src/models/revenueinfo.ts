/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

export type RevenueInfo = {
  /**
   * Total revenue in micro-units
   */
  total: string;
  /**
   * Pending revenue that is confirmed but not yet withdrawn
   */
  pending: string;
  /**
   * Already withdrawn
   */
  withdrawn: string;
  token: string;
};

/** @internal */
export const RevenueInfo$inboundSchema: z.ZodMiniType<RevenueInfo, unknown> = z
  .object({
    total: types.string(),
    pending: types.string(),
    withdrawn: types.string(),
    token: types.string(),
  });

export function revenueInfoFromJSON(
  jsonString: string,
): SafeParseResult<RevenueInfo, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => RevenueInfo$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'RevenueInfo' from JSON`,
  );
}
