/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

export type AlgorithmScore = {
  /**
   * Overall score (0-100)
   */
  overall: number;
  /**
   * Component scores (algorithm-specific)
   */
  components: { [k: string]: number };
  /**
   * Raw metadata from the computation
   */
  metadata?: { [k: string]: any } | undefined;
  /**
   * Unix timestamp
   */
  computedAt: number;
};

/** @internal */
export const AlgorithmScore$inboundSchema: z.ZodMiniType<
  AlgorithmScore,
  unknown
> = z.object({
  overall: types.number(),
  components: z.record(z.string(), types.number()),
  metadata: types.optional(z.record(z.string(), z.any())),
  computedAt: types.number(),
});

export function algorithmScoreFromJSON(
  jsonString: string,
): SafeParseResult<AlgorithmScore, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => AlgorithmScore$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'AlgorithmScore' from JSON`,
  );
}
