/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { safeParse } from "../lib/schemas.js";
import { ClosedEnum } from "../types/enums.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

export const Scheme = {
  Exact: "exact",
} as const;
export type Scheme = ClosedEnum<typeof Scheme>;

export type Payment = {
  chain: string;
  token: string;
  tokenAddress: string;
  /**
   * Amount in micro-units (6 decimals)
   */
  amount: string;
  /**
   * Wallet or splitter contract address
   */
  recipient: string;
  facilitator: string;
  scheme: Scheme;
};

export type X402 = {
  version: string;
  facilitator: string;
  description?: string | undefined;
  payment: Payment;
  alternatives?: Array<{ [k: string]: any }> | undefined;
  splits?: Array<{ [k: string]: any }> | undefined;
  /**
   * Unix timestamp
   */
  expires: number;
};

/** @internal */
export const Scheme$inboundSchema: z.ZodMiniEnum<typeof Scheme> = z.enum(
  Scheme,
);

/** @internal */
export const Payment$inboundSchema: z.ZodMiniType<Payment, unknown> = z.object({
  chain: types.string(),
  token: types.string(),
  tokenAddress: types.string(),
  amount: types.string(),
  recipient: types.string(),
  facilitator: types.string(),
  scheme: Scheme$inboundSchema,
});

export function paymentFromJSON(
  jsonString: string,
): SafeParseResult<Payment, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Payment$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Payment' from JSON`,
  );
}

/** @internal */
export const X402$inboundSchema: z.ZodMiniType<X402, unknown> = z.object({
  version: types.string(),
  facilitator: types.string(),
  description: types.optional(types.string()),
  payment: z.lazy(() => Payment$inboundSchema),
  alternatives: types.optional(z.array(z.record(z.string(), z.any()))),
  splits: types.optional(z.array(z.record(z.string(), z.any()))),
  expires: types.number(),
});

export function x402FromJSON(
  jsonString: string,
): SafeParseResult<X402, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => X402$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'X402' from JSON`,
  );
}
