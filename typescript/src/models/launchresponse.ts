/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4-mini";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import * as openEnums from "../types/enums.js";
import { OpenEnum } from "../types/enums.js";
import { Result as SafeParseResult } from "../types/fp.js";
import * as types from "../types/primitives.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

/**
 * Active verification mode
 */
export const VerificationMode = {
  Full: "full",
  Minimal: "minimal",
} as const;
/**
 * Active verification mode
 */
export type VerificationMode = OpenEnum<typeof VerificationMode>;

export type LaunchResponse = {
  success: boolean;
  /**
   * Created passport identifier
   */
  passportId?: string | undefined;
  /**
   * Deployment record identifier
   */
  deploymentId?: string | undefined;
  /**
   * Public URL of the deployed agent
   */
  deploymentUrl?: string | undefined;
  /**
   * Active verification mode
   */
  verificationMode?: VerificationMode | undefined;
  /**
   * Whether the agent is eligible for on-chain reputation scoring
   */
  reputationEligible: boolean;
  /**
   * SHA-256 hash of the launch configuration (for audit)
   */
  configHash?: string | undefined;
  /**
   * Error message (present only when success is false)
   */
  error?: string | undefined;
};

/** @internal */
export const VerificationMode$inboundSchema: z.ZodMiniType<
  VerificationMode,
  unknown
> = openEnums.inboundSchema(VerificationMode);

/** @internal */
export const LaunchResponse$inboundSchema: z.ZodMiniType<
  LaunchResponse,
  unknown
> = z.pipe(
  z.object({
    success: types.boolean(),
    passport_id: types.optional(types.string()),
    deployment_id: types.optional(types.string()),
    deployment_url: types.optional(types.string()),
    verification_mode: types.optional(VerificationMode$inboundSchema),
    reputation_eligible: types.boolean(),
    config_hash: types.optional(types.string()),
    error: types.optional(types.string()),
  }),
  z.transform((v) => {
    return remap$(v, {
      "passport_id": "passportId",
      "deployment_id": "deploymentId",
      "deployment_url": "deploymentUrl",
      "verification_mode": "verificationMode",
      "reputation_eligible": "reputationEligible",
      "config_hash": "configHash",
    });
  }),
);

export function launchResponseFromJSON(
  jsonString: string,
): SafeParseResult<LaunchResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => LaunchResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'LaunchResponse' from JSON`,
  );
}
