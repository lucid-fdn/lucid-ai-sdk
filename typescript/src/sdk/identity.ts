/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { identityLucidGetIdentity } from "../funcs/identityLucidGetIdentity.js";
import { identityLucidGetIdentityChains } from "../funcs/identityLucidGetIdentityChains.js";
import { identityLucidLinkIdentity } from "../funcs/identityLucidLinkIdentity.js";
import { identityLucidResolveIdentity } from "../funcs/identityLucidResolveIdentity.js";
import { identityLucidUnlinkIdentity } from "../funcs/identityLucidUnlinkIdentity.js";
import { ClientSDK, RequestOptions } from "../lib/sdks.js";
import * as models from "../models/index.js";
import * as operations from "../models/operations/index.js";
import { unwrapAsync } from "../types/fp.js";

export class Identity extends ClientSDK {
  /**
   * Link addresses cross-chain
   *
   * @remarks
   * Link a new blockchain address to an existing cross-chain identity using CAIP-10 format. Creates the identity graph if it does not exist. Supports both Solana and EVM addresses.
   */
  async lucidLinkIdentity(
    request: models.LinkIdentityRequest,
    options?: RequestOptions,
  ): Promise<models.IdentityLinkResponse> {
    return unwrapAsync(identityLucidLinkIdentity(
      this,
      request,
      options,
    ));
  }

  /**
   * Resolve cross-chain identity
   *
   * @remarks
   * Resolve a CAIP-10 address to its full cross-chain identity graph via POST. Returns all linked addresses across chains and the identity's creation timestamp.
   */
  async lucidResolveIdentity(
    request: models.ResolveIdentityRequest,
    options?: RequestOptions,
  ): Promise<models.SuccessResponse> {
    return unwrapAsync(identityLucidResolveIdentity(
      this,
      request,
      options,
    ));
  }

  /**
   * Resolve identity (GET)
   *
   * @remarks
   * Resolve a CAIP-10 address to its cross-chain identity graph via GET query parameter. Functionally equivalent to the POST resolve endpoint but uses query string input.
   */
  async lucidGetIdentity(
    request: operations.LucidGetIdentityRequest,
    options?: RequestOptions,
  ): Promise<models.SuccessResponse> {
    return unwrapAsync(identityLucidGetIdentity(
      this,
      request,
      options,
    ));
  }

  /**
   * Get linked chains for identity
   *
   * @remarks
   * Retrieve all blockchain chains linked to a CAIP-10 address, returning chain identifiers and their associated addresses within the identity graph.
   */
  async lucidGetIdentityChains(
    request: operations.LucidGetIdentityChainsRequest,
    options?: RequestOptions,
  ): Promise<models.IdentityChainsResponse> {
    return unwrapAsync(identityLucidGetIdentityChains(
      this,
      request,
      options,
    ));
  }

  /**
   * Unlink a cross-chain address
   *
   * @remarks
   * Remove a linked address from a cross-chain identity graph. Returns 404 if the address is not found in any identity graph.
   */
  async lucidUnlinkIdentity(
    request: models.UnlinkIdentityRequest,
    options?: RequestOptions,
  ): Promise<models.SuccessResponse> {
    return unwrapAsync(identityLucidUnlinkIdentity(
      this,
      request,
      options,
    ));
  }
}
