/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { a2ALucidDeleteA2aTask } from "../funcs/a2ALucidDeleteA2aTask.js";
import { a2ALucidDiscoverA2aAgent } from "../funcs/a2ALucidDiscoverA2aAgent.js";
import { a2ALucidGetA2aAgentCard } from "../funcs/a2ALucidGetA2aAgentCard.js";
import { a2ALucidGetA2aTask } from "../funcs/a2ALucidGetA2aTask.js";
import { a2ALucidListA2aTasks } from "../funcs/a2ALucidListA2aTasks.js";
import { a2ALucidSendA2aTask } from "../funcs/a2ALucidSendA2aTask.js";
import { ClientSDK, RequestOptions } from "../lib/sdks.js";
import * as models from "../models/index.js";
import * as operations from "../models/operations/index.js";
import { unwrapAsync } from "../types/fp.js";
import { PageIterator, unwrapResultIterator } from "../types/operations.js";

export class A2A extends ClientSDK {
  /**
   * Get A2A Agent Card
   *
   * @remarks
   * Standard A2A discovery endpoint. Returns the Agent Card for
   * the specified agent, conforming to the A2A protocol spec.
   */
  async lucidGetA2aAgentCard(
    request: operations.LucidGetA2aAgentCardRequest,
    options?: RequestOptions,
  ): Promise<models.A2AAgentCard> {
    return unwrapAsync(a2ALucidGetA2aAgentCard(
      this,
      request,
      options,
    ));
  }

  /**
   * Send task to A2A agent
   *
   * @remarks
   * Admin-only. Sends a task to an A2A-compatible agent. If the agent
   * has a deployment URL, the request is forwarded via the A2A client.
   */
  async lucidSendA2aTask(
    request: operations.LucidSendA2aTaskRequest,
    options?: RequestOptions,
  ): Promise<models.A2ATask> {
    return unwrapAsync(a2ALucidSendA2aTask(
      this,
      request,
      options,
    ));
  }

  /**
   * Discover an external A2A agent
   *
   * @remarks
   * Admin-only. Fetches the Agent Card from an external HTTPS URL.
   * Includes SSRF protection (blocks internal/private IPs).
   */
  async lucidDiscoverA2aAgent(
    request: operations.LucidDiscoverA2aAgentRequest,
    options?: RequestOptions,
  ): Promise<operations.LucidDiscoverA2aAgentResponse> {
    return unwrapAsync(a2ALucidDiscoverA2aAgent(
      this,
      request,
      options,
    ));
  }

  /**
   * Get A2A task status
   *
   * @remarks
   * Admin-only. Retrieves the current state of a task.
   */
  async lucidGetA2aTask(
    request: operations.LucidGetA2aTaskRequest,
    options?: RequestOptions,
  ): Promise<models.A2ATask> {
    return unwrapAsync(a2ALucidGetA2aTask(
      this,
      request,
      options,
    ));
  }

  /**
   * Delete an A2A task
   *
   * @remarks
   * Admin-only. Deletes a task record. If the task is still in progress it is
   * canceled first, then the record is removed. Returns 404 if the task does
   * not exist.
   */
  async lucidDeleteA2aTask(
    request: operations.LucidDeleteA2aTaskRequest,
    options?: RequestOptions,
  ): Promise<operations.LucidDeleteA2aTaskResponse> {
    return unwrapAsync(a2ALucidDeleteA2aTask(
      this,
      request,
      options,
    ));
  }

  /**
   * List A2A tasks for an agent
   *
   * @remarks
   * Admin-only. Lists tasks with optional state filter and pagination.
   */
  async lucidListA2aTasks(
    request: operations.LucidListA2aTasksRequest,
    options?: RequestOptions,
  ): Promise<
    PageIterator<operations.LucidListA2aTasksResponse, { page: number }>
  > {
    return unwrapResultIterator(a2ALucidListA2aTasks(
      this,
      request,
      options,
    ));
  }
}
