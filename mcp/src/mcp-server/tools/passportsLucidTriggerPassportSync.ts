/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { passportsLucidTriggerPassportSync } from "../../funcs/passportsLucidTriggerPassportSync.js";
import { LucidTriggerPassportSyncRequest$zodSchema } from "../../models/lucidtriggerpassportsyncop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: LucidTriggerPassportSyncRequest$zodSchema,
};

export const tool$passportsLucidTriggerPassportSync: ToolDefinition<
  typeof args
> = {
  name: "lucid_trigger_passport_sync",
  description: `Trigger on-chain sync for a passport.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await passportsLucidTriggerPassportSync(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
