/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { runLucidRunInference } from "../../funcs/runLucidRunInference.js";
import { InferenceRequest$zodSchema } from "../../models/inferencerequest.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: InferenceRequest$zodSchema,
};

export const tool$runLucidRunInference: ToolDefinition<typeof args> = {
  name: "lucid_run_inference",
  description: `Run inference through LucidLayer.
NOTE: streaming responses may not be supported by all MCP clients.
`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await runLucidRunInference(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
