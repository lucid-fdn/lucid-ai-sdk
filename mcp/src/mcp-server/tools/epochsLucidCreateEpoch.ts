/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { epochsLucidCreateEpoch } from "../../funcs/epochsLucidCreateEpoch.js";
import { LucidCreateEpochRequest$zodSchema } from "../../models/lucidcreateepochop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: LucidCreateEpochRequest$zodSchema.optional(),
};

export const tool$epochsLucidCreateEpoch: ToolDefinition<typeof args> = {
  name: "lucid_create_epoch",
  description: `Create a new epoch.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await epochsLucidCreateEpoch(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
