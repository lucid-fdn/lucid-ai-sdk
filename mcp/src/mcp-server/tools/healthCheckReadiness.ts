/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { healthCheckReadiness } from "../../funcs/healthCheckReadiness.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$healthCheckReadiness: ToolDefinition = {
  name: "check_readiness",
  description: `Readiness probe - is the application ready to serve traffic?`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  tool: async (client, ctx) => {
    const [result, apiCall] = await healthCheckReadiness(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
