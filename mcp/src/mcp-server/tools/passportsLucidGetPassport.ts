/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { passportsLucidGetPassport } from "../../funcs/passportsLucidGetPassport.js";
import { LucidGetPassportRequest$zodSchema } from "../../models/lucidgetpassportop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: LucidGetPassportRequest$zodSchema,
};

export const tool$passportsLucidGetPassport: ToolDefinition<typeof args> = {
  name: "lucid_get_passport",
  description: `Retrieve a passport by its passport_id.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await passportsLucidGetPassport(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
