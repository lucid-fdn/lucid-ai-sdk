/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { receiptsLucidGetMmrRoot } from "../../funcs/receiptsLucidGetMmrRoot.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$receiptsLucidGetMmrRoot: ToolDefinition = {
  name: "lucid_get_mmr_root",
  description: `Get the current MMR root and leaf count.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  tool: async (client, ctx) => {
    const [result, apiCall] = await receiptsLucidGetMmrRoot(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
