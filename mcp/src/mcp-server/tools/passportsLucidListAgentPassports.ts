/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { passportsLucidListAgentPassports } from "../../funcs/passportsLucidListAgentPassports.js";
import { LucidListAgentPassportsRequest$zodSchema } from "../../models/lucidlistagentpassportsop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: LucidListAgentPassportsRequest$zodSchema.optional(),
};

export const tool$passportsLucidListAgentPassports: ToolDefinition<typeof args> = {
  name: "lucid_list_agent_passports",
  description: `List active agent passports with optional filtering.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await passportsLucidListAgentPassports(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
