/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { receiptsLucidVerifyReceipt } from "../../funcs/receiptsLucidVerifyReceipt.js";
import { LucidVerifyReceiptRequest$zodSchema } from "../../models/lucidverifyreceiptop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: LucidVerifyReceiptRequest$zodSchema,
};

export const tool$receiptsLucidVerifyReceipt: ToolDefinition<typeof args> = {
  name: "lucid_verify_receipt",
  description: `Verify a receipt (hash + signature + inclusion proof).`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await receiptsLucidVerifyReceipt(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
