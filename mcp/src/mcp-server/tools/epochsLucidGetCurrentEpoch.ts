/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { epochsLucidGetCurrentEpoch } from "../../funcs/epochsLucidGetCurrentEpoch.js";
import { LucidGetCurrentEpochRequest$zodSchema } from "../../models/lucidgetcurrentepochop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: LucidGetCurrentEpochRequest$zodSchema.optional(),
};

export const tool$epochsLucidGetCurrentEpoch: ToolDefinition<typeof args> = {
  name: "lucid_get_current_epoch",
  description: `Get the current epoch.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await epochsLucidGetCurrentEpoch(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
