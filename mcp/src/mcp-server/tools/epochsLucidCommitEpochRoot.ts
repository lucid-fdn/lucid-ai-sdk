/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { epochsLucidCommitEpochRoot } from "../../funcs/epochsLucidCommitEpochRoot.js";
import { LucidCommitEpochRootRequest$zodSchema } from "../../models/lucidcommitepochrootop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: LucidCommitEpochRootRequest$zodSchema.optional(),
};

export const tool$epochsLucidCommitEpochRoot: ToolDefinition<typeof args> = {
  name: "lucid_commit_epoch_root",
  description: `Commit an epoch root to chain.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await epochsLucidCommitEpochRoot(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
