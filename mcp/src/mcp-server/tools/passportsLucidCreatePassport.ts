/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { passportsLucidCreatePassport } from "../../funcs/passportsLucidCreatePassport.js";
import { CreatePassportRequest$zodSchema } from "../../models/createpassportrequest.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: CreatePassportRequest$zodSchema,
};

export const tool$passportsLucidCreatePassport: ToolDefinition<typeof args> = {
  name: "lucid_create_passport",
  description: `Create a new passport (model/compute/tool/dataset/agent).`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await passportsLucidCreatePassport(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
