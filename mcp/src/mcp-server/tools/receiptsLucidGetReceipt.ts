/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { receiptsLucidGetReceipt } from "../../funcs/receiptsLucidGetReceipt.js";
import { LucidGetReceiptRequest$zodSchema } from "../../models/lucidgetreceiptop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: LucidGetReceiptRequest$zodSchema,
};

export const tool$receiptsLucidGetReceipt: ToolDefinition<typeof args> = {
  name: "lucid_get_receipt",
  description: `Retrieve a receipt by receipt_id.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await receiptsLucidGetReceipt(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
