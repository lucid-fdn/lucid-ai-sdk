/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { passportsLucidListPassports } from "../../funcs/passportsLucidListPassports.js";
import { LucidListPassportsRequest$zodSchema } from "../../models/lucidlistpassportsop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: LucidListPassportsRequest$zodSchema.optional(),
};

export const tool$passportsLucidListPassports: ToolDefinition<typeof args> = {
  name: "lucid_list_passports",
  description: `List passports with filtering, sorting, and pagination.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await passportsLucidListPassports(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
