/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { passportsLucidDeletePassport } from "../../funcs/passportsLucidDeletePassport.js";
import { LucidDeletePassportRequest$zodSchema } from "../../models/luciddeletepassportop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: LucidDeletePassportRequest$zodSchema,
};

export const tool$passportsLucidDeletePassport: ToolDefinition<typeof args> = {
  name: "lucid_delete_passport",
  description: `Soft-delete a passport by passport_id.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await passportsLucidDeletePassport(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
