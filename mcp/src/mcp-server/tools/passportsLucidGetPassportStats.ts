/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { passportsLucidGetPassportStats } from "../../funcs/passportsLucidGetPassportStats.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$passportsLucidGetPassportStats: ToolDefinition = {
  name: "lucid_get_passport_stats",
  description: `Get passport statistics.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  tool: async (client, ctx) => {
    const [result, apiCall] = await passportsLucidGetPassportStats(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
