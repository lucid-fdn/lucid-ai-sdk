/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { passportsLucidSearchModels } from "../../funcs/passportsLucidSearchModels.js";
import { LucidSearchModelsRequest$zodSchema } from "../../models/lucidsearchmodelsop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: LucidSearchModelsRequest$zodSchema.optional(),
};

export const tool$passportsLucidSearchModels: ToolDefinition<typeof args> = {
  name: "lucid_search_models",
  description: `Search model passports (ModelMeta filters).`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await passportsLucidSearchModels(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
