/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { agentsGetAgentHistory } from "../../funcs/agentsGetAgentHistory.js";
import { GetAgentHistoryRequest$zodSchema } from "../../models/getagenthistoryop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetAgentHistoryRequest$zodSchema,
};

export const tool$agentsGetAgentHistory: ToolDefinition<typeof args> = {
  name: "get_agent_history",
  description: `Get an agent's MMR root history across epochs.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await agentsGetAgentHistory(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
