/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { agentsValidateFlowspec } from "../../funcs/agentsValidateFlowspec.js";
import { ValidateFlowspecRequest$zodSchema } from "../../models/validateflowspecop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: ValidateFlowspecRequest$zodSchema,
};

export const tool$agentsValidateFlowspec: ToolDefinition<typeof args> = {
  name: "validate_flowspec",
  description: `Validate a FlowSpec structure without executing.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await agentsValidateFlowspec(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
