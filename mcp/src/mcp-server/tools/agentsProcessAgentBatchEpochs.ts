/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { agentsProcessAgentBatchEpochs } from "../../funcs/agentsProcessAgentBatchEpochs.js";
import { AgentBatchEpochsRequest$zodSchema } from "../../models/processagentbatchepochsop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: AgentBatchEpochsRequest$zodSchema,
};

export const tool$agentsProcessAgentBatchEpochs: ToolDefinition<typeof args> = {
  name: "process_agent_batch_epochs",
  description: `Process multiple epochs in batch for one or more agents.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await agentsProcessAgentBatchEpochs(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
