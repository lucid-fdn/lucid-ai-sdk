/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { agentsPlanAgentWorkflow } from "../../funcs/agentsPlanAgentWorkflow.js";
import { AgentPlanRequest$zodSchema } from "../../models/planagentworkflowop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: AgentPlanRequest$zodSchema,
};

export const tool$agentsPlanAgentWorkflow: ToolDefinition<typeof args> = {
  name: "plan_agent_workflow",
  description: `Plan a workflow from a natural language goal using CrewAI.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await agentsPlanAgentWorkflow(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
