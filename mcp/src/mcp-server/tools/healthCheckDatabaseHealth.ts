/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { healthCheckDatabaseHealth } from "../../funcs/healthCheckDatabaseHealth.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$healthCheckDatabaseHealth: ToolDefinition = {
  name: "check_database_health",
  description: `Check database connectivity and latency.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  tool: async (client, ctx) => {
    const [result, apiCall] = await healthCheckDatabaseHealth(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
