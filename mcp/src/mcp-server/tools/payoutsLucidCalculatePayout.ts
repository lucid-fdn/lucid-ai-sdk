/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { payoutsLucidCalculatePayout } from "../../funcs/payoutsLucidCalculatePayout.js";
import { PayoutCalculateRequest$zodSchema } from "../../models/payoutcalculaterequest.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: PayoutCalculateRequest$zodSchema,
};

export const tool$payoutsLucidCalculatePayout: ToolDefinition<typeof args> = {
  name: "lucid_calculate_payout",
  description: `Calculate payout split between recipients.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await payoutsLucidCalculatePayout(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
