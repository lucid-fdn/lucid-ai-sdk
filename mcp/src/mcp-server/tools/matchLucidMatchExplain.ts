/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { matchLucidMatchExplain } from "../../funcs/matchLucidMatchExplain.js";
import { LucidMatchExplainRequest$zodSchema } from "../../models/lucidmatchexplainop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: LucidMatchExplainRequest$zodSchema,
};

export const tool$matchLucidMatchExplain: ToolDefinition<typeof args> = {
  name: "lucid_match_explain",
  description:
    `Evaluate a policy against compute & model metadata and explain the decision.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await matchLucidMatchExplain(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
