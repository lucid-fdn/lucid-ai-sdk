/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { healthCheckLiveness } from "../../funcs/healthCheckLiveness.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$healthCheckLiveness: ToolDefinition = {
  name: "check_liveness",
  description: `Liveness probe - is the application alive?`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  tool: async (client, ctx) => {
    const [result, apiCall] = await healthCheckLiveness(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
