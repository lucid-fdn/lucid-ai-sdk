/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { computeLucidHeartbeat } from "../../funcs/computeLucidHeartbeat.js";
import { ComputeHeartbeat$zodSchema } from "../../models/computeheartbeat.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: ComputeHeartbeat$zodSchema,
};

export const tool$computeLucidHeartbeat: ToolDefinition<typeof args> = {
  name: "lucid_heartbeat",
  description: `Submit a compute node heartbeat to update live state.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await computeLucidHeartbeat(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
