/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { agentsGenerateAgentProof } from "../../funcs/agentsGenerateAgentProof.js";
import { AgentProofRequest$zodSchema } from "../../models/generateagentproofop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: AgentProofRequest$zodSchema,
};

export const tool$agentsGenerateAgentProof: ToolDefinition<typeof args> = {
  name: "generate_agent_proof",
  description: `Generate cryptographic proof of contribution for a specific vector.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await agentsGenerateAgentProof(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
