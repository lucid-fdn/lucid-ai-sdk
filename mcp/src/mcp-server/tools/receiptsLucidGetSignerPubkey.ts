/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { receiptsLucidGetSignerPubkey } from "../../funcs/receiptsLucidGetSignerPubkey.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$receiptsLucidGetSignerPubkey: ToolDefinition = {
  name: "lucid_get_signer_pubkey",
  description: `Get the orchestrator signing public key.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  tool: async (client, ctx) => {
    const [result, apiCall] = await receiptsLucidGetSignerPubkey(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
