/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { payoutsLucidGetPayout } from "../../funcs/payoutsLucidGetPayout.js";
import { LucidGetPayoutRequest$zodSchema } from "../../models/lucidgetpayoutop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: LucidGetPayoutRequest$zodSchema,
};

export const tool$payoutsLucidGetPayout: ToolDefinition<typeof args> = {
  name: "lucid_get_payout",
  description: `Get payout for a run_id.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await payoutsLucidGetPayout(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
