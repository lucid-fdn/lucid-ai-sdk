/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { agentsGetPlannerInfo } from "../../funcs/agentsGetPlannerInfo.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$agentsGetPlannerInfo: ToolDefinition = {
  name: "get_planner_info",
  description: `Get agent planner service information and status.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  tool: async (client, ctx) => {
    const [result, apiCall] = await agentsGetPlannerInfo(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
