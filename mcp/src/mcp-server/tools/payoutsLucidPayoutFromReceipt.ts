/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { payoutsLucidPayoutFromReceipt } from "../../funcs/payoutsLucidPayoutFromReceipt.js";
import { PayoutFromReceiptRequest$zodSchema } from "../../models/payoutfromreceiptrequest.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: PayoutFromReceiptRequest$zodSchema,
};

export const tool$payoutsLucidPayoutFromReceipt: ToolDefinition<typeof args> = {
  name: "lucid_payout_from_receipt",
  description: `Create payout split from receipt token data.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await payoutsLucidPayoutFromReceipt(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
