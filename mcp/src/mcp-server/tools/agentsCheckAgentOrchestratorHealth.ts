/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { agentsCheckAgentOrchestratorHealth } from "../../funcs/agentsCheckAgentOrchestratorHealth.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$agentsCheckAgentOrchestratorHealth: ToolDefinition = {
  name: "check_agent_orchestrator_health",
  description: `Check health of all agent orchestrator services.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  tool: async (client, ctx) => {
    const [result, apiCall] = await agentsCheckAgentOrchestratorHealth(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
