/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { agentsGetAgentOrchestratorHistory } from "../../funcs/agentsGetAgentOrchestratorHistory.js";
import { GetAgentOrchestratorHistoryRequest$zodSchema } from "../../models/getagentorchestratorhistoryop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetAgentOrchestratorHistoryRequest$zodSchema,
};

export const tool$agentsGetAgentOrchestratorHistory: ToolDefinition<typeof args> = {
  name: "get_agent_orchestrator_history",
  description: `Get agent execution history for a specific tenant.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await agentsGetAgentOrchestratorHistory(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
