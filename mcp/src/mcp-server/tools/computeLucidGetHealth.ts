/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { computeLucidGetHealth } from "../../funcs/computeLucidGetHealth.js";
import { LucidGetHealthRequest$zodSchema } from "../../models/lucidgethealthop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: LucidGetHealthRequest$zodSchema,
};

export const tool$computeLucidGetHealth: ToolDefinition<typeof args> = {
  name: "lucid_get_health",
  description: `Get the current health state of a compute node.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await computeLucidGetHealth(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
