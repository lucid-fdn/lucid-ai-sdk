/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { epochsLucidGetEpoch } from "../../funcs/epochsLucidGetEpoch.js";
import { LucidGetEpochRequest$zodSchema } from "../../models/lucidgetepochop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: LucidGetEpochRequest$zodSchema,
};

export const tool$epochsLucidGetEpoch: ToolDefinition<typeof args> = {
  name: "lucid_get_epoch",
  description: `Get an epoch by epoch_id.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await epochsLucidGetEpoch(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
