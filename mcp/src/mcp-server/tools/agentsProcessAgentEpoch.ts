/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { agentsProcessAgentEpoch } from "../../funcs/agentsProcessAgentEpoch.js";
import { AgentEpochRequest$zodSchema } from "../../models/processagentepochop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: AgentEpochRequest$zodSchema,
};

export const tool$agentsProcessAgentEpoch: ToolDefinition<typeof args> = {
  name: "process_agent_epoch",
  description: `Process an epoch of vectors for an AI agent.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await agentsProcessAgentEpoch(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
