/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { epochsLucidGetEpochTransaction } from "../../funcs/epochsLucidGetEpochTransaction.js";
import { LucidGetEpochTransactionRequest$zodSchema } from "../../models/lucidgetepochtransactionop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: LucidGetEpochTransactionRequest$zodSchema,
};

export const tool$epochsLucidGetEpochTransaction: ToolDefinition<typeof args> =
  {
    name: "lucid_get_epoch_transaction",
    description: `Get epoch anchoring transaction.`,
    annotations: {
      "title": "",
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": false,
      "readOnlyHint": false,
    },
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await epochsLucidGetEpochTransaction(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
