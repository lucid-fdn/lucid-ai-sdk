/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { agentsGetAgentStats } from "../../funcs/agentsGetAgentStats.js";
import { GetAgentStatsRequest$zodSchema } from "../../models/getagentstatsop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetAgentStatsRequest$zodSchema,
};

export const tool$agentsGetAgentStats: ToolDefinition<typeof args> = {
  name: "get_agent_stats",
  description: `Get statistics and current status for an AI agent.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await agentsGetAgentStats(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
