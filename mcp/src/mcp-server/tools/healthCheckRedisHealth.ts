/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { healthCheckRedisHealth } from "../../funcs/healthCheckRedisHealth.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$healthCheckRedisHealth: ToolDefinition = {
  name: "check_redis_health",
  description: `Check Redis connectivity and latency.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  tool: async (client, ctx) => {
    const [result, apiCall] = await healthCheckRedisHealth(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
