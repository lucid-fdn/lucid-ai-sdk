/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { agentsExecuteAgentFlowspec } from "../../funcs/agentsExecuteAgentFlowspec.js";
import { ExecuteAgentFlowspecRequest$zodSchema } from "../../models/executeagentflowspecop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: ExecuteAgentFlowspecRequest$zodSchema,
};

export const tool$agentsExecuteAgentFlowspec: ToolDefinition<typeof args> = {
  name: "execute_agent_flowspec",
  description: `Execute a FlowSpec with automatic executor selection.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await agentsExecuteAgentFlowspec(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
