/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { payoutsLucidVerifyPayout } from "../../funcs/payoutsLucidVerifyPayout.js";
import { LucidVerifyPayoutRequest$zodSchema } from "../../models/lucidverifypayoutop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: LucidVerifyPayoutRequest$zodSchema,
};

export const tool$payoutsLucidVerifyPayout: ToolDefinition<typeof args> = {
  name: "lucid_verify_payout",
  description: `Verify payout split integrity.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await payoutsLucidVerifyPayout(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
