/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { epochsLucidCommitEpochRootsBatch } from "../../funcs/epochsLucidCommitEpochRootsBatch.js";
import { LucidCommitEpochRootsBatchRequest$zodSchema } from "../../models/lucidcommitepochrootsbatchop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: LucidCommitEpochRootsBatchRequest$zodSchema,
};

export const tool$epochsLucidCommitEpochRootsBatch: ToolDefinition<
  typeof args
> = {
  name: "lucid_commit_epoch_roots_batch",
  description: `Commit multiple epoch roots.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await epochsLucidCommitEpochRootsBatch(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
