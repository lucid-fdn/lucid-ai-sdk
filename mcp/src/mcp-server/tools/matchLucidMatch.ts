/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { matchLucidMatch } from "../../funcs/matchLucidMatch.js";
import { LucidMatchRequest$zodSchema } from "../../models/lucidmatchop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: LucidMatchRequest$zodSchema,
};

export const tool$matchLucidMatch: ToolDefinition<typeof args> = {
  name: "lucid_match",
  description:
    `Match compute for a model given a compute catalog and policy constraints.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await matchLucidMatch(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
