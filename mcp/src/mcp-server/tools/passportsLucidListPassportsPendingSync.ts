/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { passportsLucidListPassportsPendingSync } from "../../funcs/passportsLucidListPassportsPendingSync.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$passportsLucidListPassportsPendingSync: ToolDefinition = {
  name: "lucid_list_passports_pending_sync",
  description: `List passports that are pending on-chain sync.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  tool: async (client, ctx) => {
    const [result, apiCall] = await passportsLucidListPassportsPendingSync(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
