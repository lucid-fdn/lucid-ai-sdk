/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { matchLucidRoute } from "../../funcs/matchLucidRoute.js";
import { LucidRouteRequest$zodSchema } from "../../models/lucidrouteop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: LucidRouteRequest$zodSchema,
};

export const tool$matchLucidRoute: ToolDefinition<typeof args> = {
  name: "lucid_route",
  description: `Match + resolve an executable inference endpoint for a model.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await matchLucidRoute(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
